name: Docker Build and Deploy

on:
  push:
    branches:
      - '**'

env:
  DOCKER_USERNAME: ${{ github.actor }}
  DOCKER_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
  SERVER_USERNAME: ${{ secrets.SERVER_USERNAME }}
  SERVER_IP: ${{ secrets.SERVER_IP }}
  SERVER_SSH_PRIVATE_KEY: ${{ secrets.SERVER_SSH_PRIVATE_KEY }}

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up JDK
      uses: actions/setup-java@v3
      with:
        java-version: 17
        distribution: temurin

    - name: Build Backend
      run: ./gradlew build
      working-directory: ./src/cem-cloud-backend

    - name: Build backend Docker image
      run: docker build -t backend-image ./src/cem-cloud-backend

    - name: Build frontend Docker image
      run: docker build -t frontend-image ./src/cem-cloud-frontend

    - name: 'Login to GitHub Container Registry'
      uses: docker/login-action@v2
      with:
        registry: ghcr.io
        username: ${{github.actor}}
        password: ${{secrets.GITHUB_TOKEN}}

    - name: Publish backend Docker image
      run: docker tag backend-image ghcr.io/Stuni907/backend-image:latest && docker push ghcr.io/Stuni907/backend-image:latest

    - name: Publish frontend Docker image
      run: docker tag frontend-image ghcr.io/Stuni907/frontend-image:latest && docker push ghcr.io/Stuni907/frontend-image:latest

    - name: Install SSH key
      run: |
        mkdir -p ~/.ssh
        echo "$SERVER_SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa

    - name: Copy docker-compose.yml to server
      run: scp -i ~/.ssh/id_rsa ./docker-compose.yml $SERVER_USERNAME@$SERVER_IP:/home/$SERVER_USERNAME

    - name: SSH into server and run docker-compose
      run: |
        ssh -i ~/.ssh/id_rsa $SERVER_USERNAME@$SERVER_IP 'docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD  ghcr.io && docker-compose -f /home/$SERVER_USERNAME/docker-compose.yml up -d'
